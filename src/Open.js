import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Open = ({navigation}) => {
  return (
    <View style={{
        flex:1
    }}>
      <Text style={{fontSize:40, backgroundColor:'black'}}>Open</Text>
    </View>
  )
}

export default Open

const styles = StyleSheet.create({})